name: Publish Instagram content

on:
  schedule:
    # Retry away from the top of the hour, where GitHub reports peak load.
    # The publisher waits until 19:00 Asia/Nicosia and exits after one success.
    # Multiple attempts cover runner shortages and Cyprus daylight-saving time.
    - cron: "7,27,47 16-17 * * *"
  workflow_dispatch:
    inputs:
      content_id:
        description: Content to validate or publish
        required: true
        type: choice
        options:
          - reel-03
          - post-07
          - post-08
          - post-09
      dry_run:
        description: Validate only; do not publish
        required: true
        default: true
        type: boolean
      verify_token:
        description: Verify Instagram token; do not publish
        required: true
        default: false
        type: boolean

permissions:
  contents: write

concurrency:
  group: instagram-publisher
  cancel-in-progress: false

jobs:
  publish:
    if: github.event_name == 'workflow_dispatch' || vars.IG_AUTOPUBLISH_ENABLED == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      INSTAGRAM_ACCESS_TOKEN: ${{ secrets.INSTAGRAM_ACCESS_TOKEN }}
      INSTAGRAM_USER_ID: ${{ secrets.INSTAGRAM_USER_ID }}
      INSTAGRAM_API_VERSION: ${{ vars.INSTAGRAM_API_VERSION || 'v23.0' }}

    steps:
      - name: Check out media and publisher
        uses: actions/checkout@v4

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Test Cyprus publication window
        run: node --test automation/schedule.test.mjs

      - name: Validate or publish
        shell: bash
        run: |
          if [[ "${{ github.event_name }}" == "workflow_dispatch" ]]; then
            args=("--content=${{ inputs.content_id }}")
            if [[ "${{ inputs.dry_run }}" == "true" ]]; then
              args+=("--dry-run")
            fi
            if [[ "${{ inputs.verify_token }}" == "true" ]]; then
              args+=("--verify-token")
            fi
          else
            args=("--scheduled")
          fi
          node automation/publish-instagram.mjs "${args[@]}"

      - name: Save successful publication state
        if: success() && (github.event_name == 'schedule' || (inputs.dry_run == false && inputs.verify_token == false))
        shell: bash
        run: |
          if git diff --quiet -- automation/published.json; then
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add automation/published.json
          git commit -m "Record Instagram publication"
          git pull --rebase
          git push
